name: Procesar Quini 6 con Gemini

on:
  schedule:
    - cron: '30 1 * * 1,4'
  workflow_dispatch:

jobs:
  procesar-sorteo:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install google-genai requests

      - name: Verificar instalación de paquetes
        run: |
          python -c "import google.genai; print('google-genai instalado OK')"
          python -c "import requests; print('requests instalado OK')"

      - name: Ejecutar script de procesamiento
        env:
          AI_KEY: ${{ secrets.AI_KEY }}
          GIT_TOKEN: ${{ secrets.GIT_TOKEN }}
          GIST_ID: ${{ secrets.GIST_ID }}
        run: python quini_6.py
